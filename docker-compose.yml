services:
  # PHP Application
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jobsage-app
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - ./:/var/www/html
      - ./docker/php/local.ini:/usr/local/etc/php/conf.d/local.ini
    networks:
      - jobsage-network
    depends_on:
      - db
      - redis

  # Nginx Web Server
  nginx:
    image: nginx:alpine
    container_name: jobsage-nginx
    restart: unless-stopped
    ports:
      - "7500:80"
    volumes:
      - ./:/var/www/html
      - ./docker/nginx/nginx.conf:/etc/nginx/conf.d/default.conf
    networks:
      - jobsage-network
    depends_on:
      - app

  # MySQL Database
  db:
    image: mysql:8.0
    container_name: jobsage-db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${DB_DATABASE:-resume_builder}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-secret}
      MYSQL_USER: ${DB_USERNAME:-laravel}
      MYSQL_PASSWORD: ${DB_PASSWORD:-secret}
    volumes:
      - dbdata:/var/lib/mysql
    ports:
      - "3306:3306"
    networks:
      - jobsage-network

  # Redis Cache
  redis:
    image: redis:alpine
    container_name: jobsage-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    networks:
      - jobsage-network

  # Queue Worker
  queue:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jobsage-queue
    restart: unless-stopped
    working_dir: /var/www/html
    command: php artisan queue:work --sleep=3 --tries=3
    volumes:
      - ./:/var/www/html
    networks:
      - jobsage-network
    depends_on:
      - app
      - db
      - redis

networks:
  jobsage-network:
    driver: bridge

volumes:
  dbdata:
    driver: local

